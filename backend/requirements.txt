# SentinelAI backend — minimal, zero-native-deps stack
# (sqlite3 is used from the standard library, so no ORM/DB driver is required)
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
python-multipart>=0.0.9
